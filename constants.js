let ppt = 40
// The number of pixels per tile.

let boardSize = 50
// The height and width of the board.

const types = {
    BLUE: 'blue',
    YELLOW: 'yellow',
    RED: 'red',
    PURPLE: 'purple',
    TURQOUISE: 'turqouise',
    GRAY: 'gray',
    WHITE: 'white',
    GREEN: 'green'
}
// The enums used to classify the tile types.
// Adding an enum here that has a name corresponding to a file, will automatically add
// it to the tile menu.

let pi = Math.PI
// A an easier way to access pi.

let characterMoveDelay = 100
// The delay in miliseconds between character moving actions.
